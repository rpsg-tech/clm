# ============================================
# CLM Enterprise - Environment Configuration
# ============================================
#
# SECURITY WARNING: Never commit .env files to version control!
# This is an example file. Copy to .env and fill with real values.
#
# Production Deployment Checklist:
# ✅ All required variables set
# ✅ Secrets are cryptographically random (use: openssl rand -base64 32)
# ✅ DATABASE_URL includes connection_limit parameter
# ✅ SMTP credentials configured
# ✅ CORS_ORIGINS set to actual domains (no wildcards)
#

# ============ Server Configuration ============
NODE_ENV=development
PORT=3001

# Frontend URL (REQUIRED - no default in production)
# Used for email templates, password reset links, etc.
FRONTEND_URL=http://localhost:3000

# ============ Database ============
# REQUIRED: PostgreSQL connection string
# IMPORTANT: Include connection_limit for production (recommended: 20)
#
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?connection_limit=20
# Example: postgresql://clm:secret@localhost:5432/clm_enterprise?connection_limit=20
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/clm_enterprise?connection_limit=20"

# ============ Redis ============
# REQUIRED: Redis connection string for caching and token blacklist
# Format: redis://HOST:PORT or rediss://HOST:PORT (SSL)
REDIS_URL="redis://localhost:6379"

# ============ JWT Authentication ============
# REQUIRED: JWT secrets must be different from each other
# SECURITY: Use cryptographically random strings (min 32 chars)
# Generate with: openssl rand -base64 32
#
# Example:
# JWT_SECRET="Y9vK2pX8sL4nR7mJ3cB6fT1qH5wU0dE9gA8zV2xC6nM="
# JWT_REFRESH_SECRET="P3rT7yU9iO2pA5sD8fG1hJ4kL6zX0cV3bN5mQ8wE1rT="

JWT_SECRET=your-super-secure-jwt-secret-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=15m

JWT_REFRESH_SECRET=your-different-refresh-secret-also-change-this-min-32-chars
JWT_REFRESH_EXPIRES_IN=7d

# ============ CORS Configuration ============
# REQUIRED: Comma-separated list of allowed origins
# Production: Use actual domains with HTTPS
# Development: Can use localhost
#
# Examples:
# CORS_ORIGINS=https://app.example.com,https://admin.example.com
# CORS_ORIGINS=http://localhost:3000,http://localhost:3002
CORS_ORIGINS=http://localhost:3000,http://localhost:3002

# ============ Email Service (SMTP) ============
# REQUIRED IN PRODUCTION: Email notifications won't work without these
#
# Common SMTP providers:
# - Gmail: smtp.gmail.com (port 587, TLS)
# - SendGrid: smtp.sendgrid.net (port 587)
# - AWS SES: email-smtp.us-east-1.amazonaws.com (port 587)
# - Mailgun: smtp.mailgun.org (port 587)

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=notifications@example.com
SMTP_PASS=your-smtp-password

EMAIL_FROM=noreply@clm-enterprise.com
EMAIL_FROM_NAME=CLM Enterprise

# ============ AI Features (Optional) ============
# For contract analysis and AI-powered features
# Get key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# ============ AWS S3 (Optional) ============
# For document storage (if using S3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=clm-enterprise-documents

# ============ Approval Workflow ============
# Email addresses for approval notifications
# These receive contract submission notifications
LEGAL_APPROVER_EMAIL=legal@yourcompany.com
FINANCE_APPROVER_EMAIL=finance@yourcompany.com

# ============ Logging & Monitoring (Optional) ============
LOG_LEVEL=info

# Sentry (Error tracking)
# SENTRY_DSN=https://xxx@sentry.io/xxx

# ============ Development Only ============
# These are only used in development/staging
DEBUG=false

