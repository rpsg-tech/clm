# MinIO Environment Configuration (TODO — for future use)
# This file is deployed to /etc/default/minio by 05-minio-setup.sh
# Passwords are auto-generated at setup time; these are placeholders.

MINIO_ROOT_USER=clm-minio-admin
MINIO_ROOT_PASSWORD=CHANGE_ME_GENERATED_AT_SETUP
MINIO_VOLUMES=/mnt/data/minio
MINIO_OPTS="--console-address :9001"
